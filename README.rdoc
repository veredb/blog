== README

This is a Ruby on Rails project. It contains the ability to create posts and comment on each post. (it contains two models: posts, comments)
It has the principle of MVC and RESTful design.
It has a MySQL database.
It is set up with an Active Record association, which uses the relation ship has_many, belongs_to.
It is supported by Rspec as TDD
